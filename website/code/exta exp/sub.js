document.getElementById('myForm').addEventListener('submit', function(event) {
  event.preventDefault(); // Prevent the form from submitting in the default way
  
  // Navigate to another page
  window.location.href = 'indihome.html';
});
